const path = require('path');

module.exports = {
// Add font in the locations
// And specify the path below assets sections
// Then run the command `npx react-native-asset`
// And set the font using ASAP SDK in native code
// To set the font in the specified files:
// For iOS, update `ios/AppDelegate.m`
// For Android, update `android/app/src/main/java/[...]/MainActivity.kt`
 assets: ['./assets/fonts/'],
};